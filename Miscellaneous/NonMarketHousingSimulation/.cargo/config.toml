# Set the getrandom backend for the WASM target.
# Required by `rand` / `getrandom` 0.3 when targeting wasm32-unknown-unknown;
# without this the crate will fail to compile for the web build.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
